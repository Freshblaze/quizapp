class QuizQuestion {
  final String text;
  final List<String> answer;
  QuizQuestion(this.text,
      this.answer); //so you will be able to accept the variable outside the class without instatiating it
}
